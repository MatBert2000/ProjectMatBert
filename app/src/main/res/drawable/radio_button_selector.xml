<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Активное состояние (выбрано) -->
    <item android:state_checked="true" android:color="#C0FD14" />
    <!-- Неактивное состояние (не выбрано) -->
    <item android:state_checked="false" android:color="#EDF4E9" />
    <!-- Состояние по умолчанию -->
    <item android:color="#EDF4E9" />
</selector>